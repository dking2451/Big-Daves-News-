# Future features (backlog)

Ideas captured for after testing—implement only if they still feel necessary.

## Watch tab

- **Carousel → grid focus (iPhone)**  
  When the user taps a title in **“New Episodes for You”**, scroll the main list so that show’s row is visible (e.g. `ScrollViewReader` + stable `id` on each card). Currently split iPad updates selection; phone carousel tap is a no-op for navigation.

---

*Add new bullets here as needed.*
